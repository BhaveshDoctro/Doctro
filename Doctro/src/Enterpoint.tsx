import React, {useEffect} from 'react';
import AppNavigation from './services/navigation/NavigationStack';
const UI = () => {
  useEffect(() => {}, []);
  return <AppNavigation />;
};

export default UI;
